# Search-R1 + Qwen3-0.6b 依赖锁定文件
# 此文件锁定所有依赖版本以确保可重现性
#
# 安装方式:
#   pip install -r requirements.txt
#
# 更新依赖（如需）:
#   pip install pip-tools
#   pip-compile requirements.in --upgrade

# 深度学习框架
torch==2.4.0

# vLLM 和相关依赖
vllm==0.6.3
# 注意: vllm-flash-attn==2.6.2 会作为 vllm 的依赖自动安装

# Transformers
transformers>=4.40.0,<5.0.0

# Web 服务
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# 实验跟踪
wandb>=0.16.0,<1.0.0

# 环境变量管理
python-dotenv>=1.0.0,<2.0.0

# Flash Attention 2（可选，如果 vllm 未自动安装）
# flash-attn>=2.0.0
